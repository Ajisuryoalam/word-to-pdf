[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn --chdir web app:app --bind 0.0.0.0:\ --workers 2 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "word-to-pdf"
