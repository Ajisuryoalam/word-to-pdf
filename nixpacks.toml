[phases.setup]
nixPkgs = ["libreoffice", "python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn --chdir web app:app --bind 0.0.0.0:\ --workers 2 --timeout 120"
